#include <stdio.h>

#define MAX_SIZE 10

int input(int *a, int *n, int *shift);
void shift_array(int *a, int n, int shift);
void output(int *a, int n);

int main(void) {
    int a[MAX_SIZE];
    int n, shift;

    if (input(a, &n, &shift)) {
        shift_array(a, n, shift);
        output(a, n);
    } else {
        printf("n/a\n");
    }

    return 0;
}

int input(int *a, int *n, int *shift) {
    int i, temp, result = 1;
    char c;

    if (scanf("%d", n) != 1 || *n <= 0 || *n > MAX_SIZE) {
        result = 0;
    }

    for (i = 0; i < *n && result; i++) {
        if (scanf("%d", &temp) != 1) {
            result = 0;
        }
        a[i] = temp;
    }

    if (result && scanf("%d", shift) != 1) {
        result = 0;
    }

    while ((c = getchar()) != '\n' && c != EOF) {
        if (c != ' ' && c != '\t') {
            result = 0;
        }
    }

    return result;
}

void shift_array(int *a, int n, int shift) {
    int b[MAX_SIZE];
    int i, index;
    int norm_shift;

    // приведение сдвига к диапазону 0..n-1
    if (shift >= 0) {
        norm_shift = shift % n;
    } else {
        norm_shift = n + (shift % n);
        if (norm_shift == n) {
            norm_shift = 0;
        }
    }

    for (i = 0; i < n; i++) {
        index = (i + norm_shift) % n;
        b[i] = a[index];
    }

    for (i = 0; i < n; i++) {
        a[i] = b[i];
    }
}

void output(int *a, int n) {
    int i;
    for (i = 0; i < n; i++) {
        printf("%d", a[i]);
        if (i < n - 1) {
            printf(" ");
        }
    }
    printf("\n");
}
